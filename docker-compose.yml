services:
  backend:
    build: ./backend
    env_file: .env
    ports:
      - "8000:8000"
    command: uvicorn main:app --host 0.0.0.0 --port 8000

  telegram_bot:
    build: ./backend
    env_file: .env
    command: python bot.py

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
